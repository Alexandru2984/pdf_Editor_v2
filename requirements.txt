asgiref==3.11.0
dj-database-url>=2.1.0
# Pinned to the 5.2 LTS series (support window ends April 2028). 6.x is
# non-LTS and drops Python 3.10/3.11 from the CI matrix — let Dependabot
# keep patching 5.2.* but not jump to 6. See .github/dependabot.yml for
# the matching ignore rule.
# Floor bumped to 5.2.15: 5.2.14 carried PYSEC-2026-197..201. Stay on the
# 5.2.* LTS line; Dependabot keeps patching within it.
Django>=5.2.15,<5.3
django-axes>=8.0.0
django-ratelimit>=4.1.0
celery[redis]>=5.4.0
# clamd client for optional ClamAV upload scanning (pdfeditor/scanning.py).
clamd>=1.0.2
django-prometheus>=2.4.1
djangorestframework>=3.17.1
drf-spectacular>=0.27.0
fastembed>=0.4.0
pgvector>=0.3.6
httpx>=0.27.0
packaging==25.0
pillow==12.2.0
psycopg2-binary>=2.9.9
pdf2docx>=0.5.13
pyHanko[image-support]==0.35.0
PyMuPDF>=1.27.0,<2.0
# TOTP MFA: secret/code handling (pyotp) + enrolment QR codes (qrcode).
pyotp>=2.9.0
qrcode[pil]>=7.4.0
pytesseract==0.3.13
python-dotenv>=1.0.0
requests>=2.28.0
sentry-sdk[django]>=2.0.0
sqlparse>=0.5.4
uvicorn[standard]>=0.30.0
# Transitive deps (pulled in via requests/httpx) pinned to a CVE-free floor:
#   urllib3 <2.7.0  → PYSEC-2026-141/142
#   idna    <3.15   → CVE-2026-45409
urllib3>=2.7.0
idna>=3.15
