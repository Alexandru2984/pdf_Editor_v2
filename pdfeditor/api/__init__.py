"""REST API package for PDF Editor.

Exposes the same operations available in the web UI under ``/api/v1/`` with
token-based authentication (``X-API-Key`` header). Schema docs live at
``/api/docs/`` (Swagger) and ``/api/redoc/`` (Redoc).
"""
